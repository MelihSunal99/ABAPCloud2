projection;
strict ( 2 );
use draft;
define behavior for Z05_C_AVCCON alias Connection
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;


  field ( readonly )
    CityFrom,
    CountryFrom,
    CityTo,
    CountryTo;

}